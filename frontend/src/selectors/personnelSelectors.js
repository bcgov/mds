import * as personnelReducer from '@/reducers/personnelReducer';

export const getPersonnel = (state) => personnelReducer.getPersonnel(state);
export const getPersonnelIds = (state) => personnelReducer.getPersonnelIds(state);