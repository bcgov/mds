import React from 'react';
import MineRecordForm from '@/components/Forms/MineRecordForm';

const MineRecordModal = (props) => {
  return (
      <MineRecordForm {...props}/> 
  )
}
 
export default MineRecordModal;