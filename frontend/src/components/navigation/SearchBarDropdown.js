import React from "react";
import PropTypes from "prop-types";

const propTypes = {};

export const SearchBarDropdown = (props) => <div>CoolStuff</div>;

SearchBarDropdown.propTypes = propTypes;
export default SearchBarDropdown;
