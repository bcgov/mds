import * as authenticationReducer from "../reducers/authenticationReducer";

export const {
  isAuthenticated,
  getUserAccessData,
  getUserInfo,
  getRedirect,
  isProponent,
} = authenticationReducer;
