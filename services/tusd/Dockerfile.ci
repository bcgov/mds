FROM tusproject/tusd:v1.3.0

COPY init.sh .

RUN mkdir -p /srv/tusd-data/data
USER root
RUN chgrp -R 0 /srv/tusd-data/data && \
    chmod -R g=u /srv/tusd-data/data

USER 1001
RUN chown -R 1001:0 /srv/tusd-data/data
ENTRYPOINT [ "./init.sh" ]
